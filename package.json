{
  "name": "agenda-familial-supabase-github",
  "version": "4.1.1",
  "private": true,
  "description": "PWA premium privée d’agenda familial partagé — v4.1.1 Ambiance dynamique & micro-détails.",
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/check-project.js",
    "build": "node scripts/build-pages.js",
    "test": "node scripts/check-project.js && node scripts/test-store.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
