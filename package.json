{
  "name": "agenda-familial-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "PWA premium d'agenda familial partagé, locale-first et synchronisable en temps réel.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/store.js && node --check js/app.js && node --check service-worker.js"
  },
  "engines": {
    "node": ">=18"
  }
}
